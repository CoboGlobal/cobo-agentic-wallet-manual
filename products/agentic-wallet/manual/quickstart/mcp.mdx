---
title: "MCP Server"
description: "5-minute MCP quickstart with stdio server, policy denial, self-correction, and audit checks."
---

Use this path when your runtime already supports MCP (Claude Desktop, Claude Code, Cursor, and similar clients).

## 5-minute outcome

1. Start the Cobo MCP stdio server
2. Invoke wallet tools from your MCP client
3. Observe policy denial returned as tool content
4. Retry with compliant params from `suggestion`
5. Confirm via audit logs

## Step 1: Install MCP extra

```bash
pip install "cobo-agent-wallet[mcp]"
```

## Step 2: Configure API access

```bash
export AGENT_WALLET_API_URL=http://localhost:8000
export AGENT_WALLET_API_KEY=your-api-key
```

## Step 3: Start stdio MCP server

```bash
python -m cobo_agentic_wallet.mcp
```

## Step 4: Register server in MCP client

Example (`claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "cobo-agent-wallet": {
      "command": "python",
      "args": ["-m", "cobo_agentic_wallet.mcp"],
      "env": {
        "AGENT_WALLET_API_URL": "http://localhost:8000",
        "AGENT_WALLET_API_KEY": "your-api-key"
      }
    }
  }
}
```

## Step 5: Run denial plus correction sequence

In your MCP client, execute this flow:

1. `list_wallets`
2. `transfer_tokens` with compliant amount (success)
3. `transfer_tokens` with too-large amount (policy denial)
4. Parse `suggestion` from tool output and retry with smaller amount
5. `get_audit_logs` to verify allowed plus denied events

Policy denials are intentionally returned as normal tool content (not protocol-level tool failures), so the LLM can reason and retry.

## Quick reference tool set

- `list_wallets`
- `get_balance`
- `transfer_tokens`
- `list_transactions`
- `get_audit_logs`
- `create_delegation`
- `evaluate_policy`

<Card title="Full MCP Integration Guide" icon="plug" href="/integrations/mcp">
  Includes advanced config and behavior details.
</Card>
